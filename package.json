{
  "name": "tank-damage-simulator",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --pretty false",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
